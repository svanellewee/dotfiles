#!/usr/bin/env bash
CTERM="\e[0m"
CYAN="\e[36m"
DADJOKE_FILE=`date +/tmp/dad_%d%m%y`
if [ ! -f "${DADJOKE_FILE}" ]; then
  curl -s -L http://icanhazdadjoke.com > "${DADJOKE_FILE}";
  xclip -selection c < "${DADJOKE_FILE}"
fi
printf "${CYAN}%s${CTERM}\n" "`cat ${DADJOKE_FILE}`"
